# 项目快速上手提示词

你正在参与一个 ROS1 机械臂阻尼控制工程项目。请先执行以下步骤，快速掌握工程全貌后再接受任务。

## 第一步：阅读核心文件

按顺序阅读：

1. `hxj_duoji_node.py` — 唯一核心源文件，全部逻辑在这一个文件里
2. `config/example.json` — 机械臂参数配置，是所有逻辑的数据源头
3. `tests/test_hxj_duoji_node.py` — 单元测试，反映代码实际行为
4. `fashionstar_servo_python_sdk_api.md` — 舵机官方 SDK API 文档

## 第二步：把握模块化核心思想

这个工程的灵魂是 **JSON 驱动的模块化可塑性**。以下是设计层面的核心原则，理解这些比读懂具体代码更重要：

### 1. 数据驱动，不硬编码
机械臂有多少轴、每个关节是什么类型、杆长多少——全部由 `config/example.json` 决定。代码里不写死舵机数量、不写死关节构型。`dof=3` 就生成 3 个 servo_id，`dof=7` 就生成 7 个，换 JSON 不改代码。

### 2. 轴向/切向解耦
关节按 `type` 字段分两类，处理方式完全不同：
- **轴向（axial）**：旋转轴沿连杆纵轴，改变下游平面朝向但不参与平面内运动学。固定基础阻尼，不参与逆解。
- **切向（tangential）**：构成平面 N-R 串联机构。构建 DH 表 → 正运动学 → 雅可比 → 阻尼分发。

两类关节在阻尼解算上完全解耦，互不干扰。

### 3. 阻尼是功率，不是力矩
这款舵机的 `set_damping(id, power)` 只能延缓角度变化，不输出力矩。所以：
- 阻尼功率直接是 mW，不经过任何扭矩转换
- 末端阻尼是操作手感优化，不是重力补偿，不计算重力扭矩
- 给一个总预算（如 1000mW），按运动学贡献分给各切向关节

### 4. 雅可比权重分发
末端阻尼预算按雅可比列范数（杠杆系数）分配到各切向关节：杠杆高的关节转一点就能大幅移动末端，分到更多功率；杠杆低的分到较少。预算守恒，位姿变化时自动重新分配。N=2 到 N=6 同一套公式，不特判。

### 5. 单文件三职责
整个工程一个 `.py` 文件，内部三个类各司其职：配置管理（读 JSON + 串口参数）、ROS 发布（只读共享状态）、控制线程（唯一访问串口 + 阻尼解算）。线程间用共享字典 + 互斥锁通信。

## 第三步：运行环境

- WSL2 Ubuntu 26.04，Docker 容器 `ros1-noetic`（ROS Noetic + Python 3.8）
- 工作空间挂载：`~/ros-workspaces/ros1_ws` → 容器内 `/root/ros1_ws`
- git 分支：WH
- 中文注释，中文回复

运行测试：
```bash
docker exec ros1-noetic bash -lc 'source /opt/ros/noetic/setup.bash && cd /root/ros1_ws/LRrobot/src/LeRobot-Anything-U-Arm-main && PYTHONPATH=.:$PYTHONPATH python3 -m unittest tests.test_hxj_duoji_node -v'
```

## 第四步：确认理解

读完文件后，用你自己的话简述：
1. 为什么轴向和切向要解耦处理？
2. 阻尼功率为什么不经过力矩转换？
3. 这个工程怎么实现 3-7 轴自适应的？

然后说"我已理解，请给任务。"
